# AI Writing Helper

🤖 中文AI文本检测与人性化工具

一个完全基于规则的本地工具，用于检测和改写AI生成的中文文本，让文本更自然、更具人性化。

## ✨ 特性

### 🔍 AI文本检测
- **多维度分析**：从词汇、句式、结构、语义四个层面全面检测
- **详细报告**：提供综合得分、置信度和各维度详细分析
- **规则引擎**：基于100+个AI高频词汇和模式规则

### ✍️ 文本人性化
- **三种强度**：轻度、中度、重度，满足不同需求
- **智能改写**：词汇替换、句式调整、添加个性化表达
- **保持语义**：在改写的同时保持原文的核心意思

### 🛡️ 隐私安全
- **完全本地**：所有处理在本地完成，无需联网
- **无数据上传**：您的文本不会被发送到任何服务器
- **开源透明**：代码完全开源，可以自行审查

## 📦 安装

### 方式1：使用pip安装（推荐）

```bash
pip install -r requirements.txt
pip install -e .
```

### 方式2：直接使用

```bash
# 克隆仓库
git clone https://github.com/ye4wzp/ai-writing-helper.git
cd ai-writing-helper

# 安装依赖
pip install -r requirements.txt
```

## 🚀 使用方式

### 1️⃣ 命令行工具（CLI）

#### 检测文本

```bash
# 检测直接输入的文本
ai-humanizer detect "在当今社会，人工智能技术发挥着至关重要的作用。"

# 检测文件中的文本
ai-humanizer detect -f input.txt

# 显示详细报告
ai-humanizer detect -f input.txt --report
```

#### 人性化处理

```bash
# 处理文本（默认中度）
ai-humanizer humanize -f input.txt -o output.txt

# 指定处理强度
ai-humanizer humanize -f input.txt -o output.txt -i light    # 轻度
ai-humanizer humanize -f input.txt -o output.txt -i medium   # 中度（推荐）
ai-humanizer humanize -f input.txt -o output.txt -i heavy    # 重度

# 显示对比报告
ai-humanizer humanize -f input.txt --compare
```

#### 一键处理

```bash
# 检测并人性化处理
ai-humanizer process -f input.txt -o output.txt

# 指定处理强度
ai-humanizer process -f input.txt -o output.txt -i heavy
```

#### 查看帮助

```bash
ai-humanizer --help
ai-humanizer detect --help
ai-humanizer humanize --help
```

### 2️⃣ Web界面

启动Web服务：

```bash
# 默认端口5000
ai-humanizer web

# 指定端口
ai-humanizer web -p 8080

# 启用调试模式
ai-humanizer web --debug
```

然后在浏览器中访问：`http://127.0.0.1:5000`

Web界面提供：
- 🎯 AI文本检测
- ✨ 文本人性化处理
- 🔄 一键处理（检测+人性化）
- 📊 可视化的分析结果
- 💾 导出处理结果

### 3️⃣ Python API

```python
from ai_humanizer import AIDetector, AIHumanizer

# 创建检测器和处理器
detector = AIDetector()
humanizer = AIHumanizer()

# 检测文本
text = "在当今社会，人工智能技术发挥着至关重要的作用。"
result = detector.detect(text)
print(f"AI得分: {result['score']:.2f}/100")
print(f"是否为AI: {result['is_ai']}")

# 生成详细报告
report = detector.generate_report(result)
print(report)

# 人性化处理
humanized = humanizer.humanize(text, intensity='medium')
print(f"处理后: {humanized['text']}")
print(f"修改次数: {humanized['change_count']}")

# 批量处理
texts = ["文本1", "文本2", "文本3"]
results = humanizer.batch_humanize(texts, intensity='medium')

# 对比报告
comparison = humanizer.compare(text, humanized)
print(comparison)
```

## 📖 API文档

### AIDetector（检测器）

#### `detect(text)`
检测文本的AI特征

**参数：**
- `text` (str): 要检测的文本

**返回：**
```python
{
    'is_ai': bool,              # 是否为AI生成
    'confidence': float,        # 置信度 (0-1)
    'score': float,            # 综合得分 (0-100)
    'text_length': int,        # 文本长度
    'summary': {
        'level': str,          # AI特征等级
        'conclusion': str,     # 结论
        'main_issue': str      # 主要问题
    },
    'details': {
        'lexical': {...},      # 词汇层面分析
        'syntactic': {...},    # 句式层面分析
        'structural': {...},   # 结构层面分析
        'semantic': {...}      # 语义层面分析
    }
}
```

#### `generate_report(detection_result)`
生成详细的检测报告

**参数：**
- `detection_result` (dict): detect()方法返回的结果

**返回：** 格式化的报告文本 (str)

### AIHumanizer（人性化处理器）

#### `humanize(text, intensity='medium')`
将文本人性化

**参数：**
- `text` (str): 要处理的文本
- `intensity` (str): 处理强度
  - `'light'`: 轻度处理，只替换最明显的AI词汇
  - `'medium'`: 中度处理，替换词汇并调整部分句式（推荐）
  - `'heavy'`: 重度处理，全面改写，添加个性化表达

**返回：**
```python
{
    'text': str,               # 处理后的文本
    'original_length': int,    # 原文长度
    'modified_length': int,    # 处理后长度
    'changes': list,           # 修改详情列表
    'change_count': int,       # 修改次数
    'intensity': str           # 处理强度
}
```

#### `batch_humanize(texts, intensity='medium')`
批量处理文本

**参数：**
- `texts` (list): 文本列表
- `intensity` (str): 处理强度

**返回：** 结果列表 (list)

#### `compare(original, humanized_result)`
比较原文和处理后的文本

**参数：**
- `original` (str): 原始文本
- `humanized_result` (dict): humanize()方法返回的结果

**返回：** 格式化的对比报告 (str)

## 🎯 效果示例

### 示例1：检测AI文本

**输入：**
```
在当今社会，人工智能技术发挥着至关重要的作用。首先，它能够有效地提升工作效率。
其次，它可以显著地优化资源配置。最后，它还能够深入地改善用户体验。
综上所述，人工智能具有重要意义。
```

**检测结果：**
- AI得分：**85.6/100**
- 置信度：**85.6%**
- 结论：文本具有非常明显的AI生成特征
- 主要问题：词汇层面的AI特征最为明显

### 示例2：人性化处理

**原文（AI味浓）：**
```
在当今社会，学习编程变得尤为重要。首先，编程能够有效地提升逻辑思维能力。
其次，它可以显著地增强问题解决能力。
```

**处理后（中度）：**
```
现在啊，学习编程变得特别重要。第一个，编程能提升逻辑思维能力。
另外，它也能增强问题解决能力。
```

**修改详情：**
- 词汇替换: '在当今社会' → '现在'
- 词汇替换: '尤为重要' → '特别重要'
- 词汇替换: '首先' → '第一个'
- 添加语气词: '啊'
- 句式调整: 打破固定句式

## 🔧 处理强度对比

| 强度 | 特点 | 适用场景 | 改动程度 |
|------|------|----------|---------|
| **轻度 (light)** | 保守处理，只替换最明显的AI词汇 | 需要保持正式风格的文档 | ⭐ |
| **中度 (medium)** | 平衡的处理，替换词汇+调整句式 | 大多数场景（推荐） | ⭐⭐⭐ |
| **重度 (heavy)** | 全面改写，添加口语化表达 | 需要非常自然的文本 | ⭐⭐⭐⭐⭐ |

## 🧠 检测原理

### 1. 词汇层面
- AI高频词（如"至关重要"、"显而易见"）
- 过于正式的连接词（如"综上所述"、"毋庸置疑"）
- 书面语词汇（如"诸如"、"倘若"）
- 限定词（如"某种"、"一定"）

### 2. 句式层面
- 固定句式（如"不仅...而且..."、"首先...其次...最后..."）
- 排比句
- 过长或过短的句子
- 列表式表达

### 3. 结构层面
- 总分总结构
- 段落结构是否规整
- 是否使用列表式组织

### 4. 语义层面
- 限定词密度
- 情感词密度（AI文本通常较少）
- 第一人称使用（AI文本通常较少）

## 📁 项目结构

```
ai-writing-helper/
├── ai_humanizer/           # 核心模块
│   ├── __init__.py
│   ├── detector.py         # AI检测器
│   ├── humanizer.py        # 人性化处理器
│   └── rules/              # 规则库
│       ├── __init__.py
│       ├── ai_keywords.py  # AI关键词库
│       ├── patterns.py     # 模式库
│       └── replacements.py # 替换规则
├── cli/                    # 命令行工具
│   ├── __init__.py
│   └── main.py
├── web/                    # Web应用
│   ├── __init__.py
│   └── app.py
├── tests/                  # 测试
│   ├── __init__.py
│   └── test_basic.py
├── examples/               # 示例
│   └── example_usage.py
├── README.md
├── requirements.txt
├── setup.py
└── .gitignore
```

## 🧪 运行测试

```bash
# 运行所有测试
python -m pytest tests/

# 运行基础测试
python tests/test_basic.py

# 运行示例
python examples/example_usage.py
```

## 🤝 贡献

欢迎贡献！请随时提交Issue或Pull Request。

### 贡献指南
1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 📝 待办事项

- [ ] 支持更多AI写作模式检测
- [ ] 增加更多替换规则
- [ ] 优化检测算法
- [ ] 添加更多测试用例
- [ ] 支持英文文本检测
- [ ] 开发浏览器插件
- [ ] 提供Docker镜像

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- [jieba](https://github.com/fxsjy/jieba) - 中文分词
- [Click](https://click.palletsprojects.com/) - 命令行框架
- [Flask](https://flask.palletsprojects.com/) - Web框架

## 📧 联系方式

- 作者：ye4wzp
- 项目主页：https://github.com/ye4wzp/ai-writing-helper

## ⚠️ 免责声明

本工具仅供学习和研究使用。检测结果仅供参考，不能作为判断文本是否由AI生成的唯一依据。请合理使用本工具，尊重原创，遵守相关法律法规。

---

如果这个项目对你有帮助，请给个 ⭐ Star！
